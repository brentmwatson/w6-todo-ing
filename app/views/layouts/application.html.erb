<!DOCTYPE html>
<html>
    <head>
        <title>W6TodoIng</title>
        <%= csrf_meta_tags %>

        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    </head>

    <body>
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                    <%= link_to 'ToDo-ing', root_path, class:"navbar-brand"%>
                </div>
                <ul class="nav navbar-nav navbar-right">
                    <li class="active">
                        <%= link_to 'Sign up', new_user_path, class:"navbar-link navbar-text"%>
                    </li>
                <li>
                    <%= link_to "Sign-out", logout_path, class:"navbar-link navbar-text"%>
                </li>
                <li>
                    <%= link_to 'New List', new_list_path, class:"navbar-link navbar-text"%>
                </li>
                </ul>
            </div>
        </nav>

        <% if flash[:notice] %>
        <div class="notice"><%= flash[:notice] %></div>
        <% end %>
        <%= yield %>
    </body>
</html>
